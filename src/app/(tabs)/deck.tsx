import { PlaceholderScreen } from "@/components/PlaceholderScreen";

export default function DeckScreen() {
  return (
    <PlaceholderScreen
      phase="Phase 6 · Mission Control"
      title="Deck"
      subtitle="Your launch command center — next move, readiness, fuel, and today's action."
    />
  );
}
