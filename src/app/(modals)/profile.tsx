import { PlaceholderScreen } from "@/components/PlaceholderScreen";

export default function ProfileModal() {
  return (
    <PlaceholderScreen
      phase="Phase 12 · Profile"
      title="Profile"
      subtitle="Astro full-body avatar, plan, Fuel, streak, level, and service ribbons."
    />
  );
}
