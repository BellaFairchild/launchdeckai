import { PlaceholderScreen } from "@/components/PlaceholderScreen";

export default function CargoModal() {
  return (
    <PlaceholderScreen
      phase="Phase 10 · Cargo Bay"
      title="Cargo Bay"
      subtitle="Your saved launch assets, grouped by type and approval status."
    />
  );
}
