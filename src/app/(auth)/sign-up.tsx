import { PlaceholderScreen } from "@/components/PlaceholderScreen";

export default function SignUpScreen() {
  return (
    <PlaceholderScreen
      phase="Phase 4 · Auth"
      title="Create Account"
      subtitle="New crew sign-up via Clerk, then straight into onboarding."
    />
  );
}
